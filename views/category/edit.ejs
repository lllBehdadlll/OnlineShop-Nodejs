<div class="d-flex justify-content-between flex-wrap flex-md nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
  <h1 class="h2">Editing: <%= category.Name %></h1>
  <div class="btn-toolbar mb-2 mb-md 0">
  </div>
</div>

<div class="col py-3">
  <div class="row">
    <div class="col">
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="/admin/category/">دسته بندی</a></li>
          <li class="breadcrumb-item active"><%= category.Name %></li>
        </ol>
      </nav>
    </div>
    <div class="col text-end fw-lighter">
      <b>آخرین آپدیت: <%= new Date(category.updatedAt).toUTCString()  %></b>
      <b>شماره دسته بندی:</b> <%= category._id %>
    </div>
  </div>

</div>


<form action="/admin/category/edit/<%= category._id %>?_method=PUT" method="POST">

  <div class="row form-group mb-4">


    <div class="col">
      <label for="picture">آدرس تصویر</label>
      <input type="text" class="form-control" id="picture" name="picture" value="<%= category.Picture %>" placeholder="آدرس تصویر دسته بندی" required>
    </div>

    <div class="col">
      <label for="name">نام دسته</label>
      <input type="text" class="form-control" id="name" name="name" value="<%= category.Name %>" placeholder="نام دسته بندی" required>
    </div>

    
  </div>

  <div class="row form-group mb-4">
    <div class="col">
      <label for="picturealt">Picture Alt</label>
      <input type="text" class="form-control" id="picturealt" name="picturealt" value="<%= category.PictureAlt %>" placeholder="Picture Alt" required>
    </div>

    <div class="col">
      <label for="picturetitle">Picture Title</label>
      <input type="text" class="form-control" id="picturetitle" name="picturetitle" value="<%= category.PictureTitle %>" placeholder="Picture Title" required>
    </div>
  </div>

  <div class="form-group mb-4">
    <button type="submit" class="btn btn-primary">آپدیت دسته بندی</button>

    <button type="button" class="btn btn-danger" data-bs-toggle="modal" id="deleteButton" data-bs-target="#deleteModal">حذف دسته بندی</button>

  </div>

</form>



<div class="modal fade" tabindex="-1" role="dialog" id="deleteModal">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <div class="modal-title">شما قصد حذف این دسته بندی را دارید؟</div>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <p>
           شما در حال حذف دسته بندی با نام هستید<b class="fw-bold"><%= category.Name %> </b><br/>
          آیا مطمئن هستید؟
        </p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">بستن</button>
        <form action="/admin/category/edit/<%= category._id %>?_method=DELETE" method="POST" class="position-relative">
          <button type="submit" class="btn btn-primary">بله حذف شود</button>
        </form>
      </div>
    </div>
  </div>
</div>